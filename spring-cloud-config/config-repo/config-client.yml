# Базовая конфигурация клиентского приложения config-client
# Используется, когда активный профиль не задан или не найден

app:
  message: "Конфигурация получена с Config Server (профиль по умолчанию)"
  environment: default

server:
  port: 8080
